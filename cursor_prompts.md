# Prompt for initial UI scaffolding - (Aishik Bandyopadhyay)

I already have a barebones frontend for a LLM Chat Interface inside @Frontend_React . Modify the existing code (potentially rewrite,if required) in order to create a clone of the UI of chatgpt.com as shown in the attached image. Make sure to include the sidebar containing all the previous chats, the search option button under the text input. Divide everything into meaningful components under the components directory, so that things are maintainable and modular. make sure to write javascript code to access the backend api at http://localhost:8000/api/generate, which receives the user prompt, the option for deep research (enabled? yes or no) as JSON, and returns the entire ai response as JSON. make sure that the UI handles this backend integration. Write the code in easily understandable fashion and explain everything clearly in layman's terms as well. (+ attached images of the UI of chatgpt.com)